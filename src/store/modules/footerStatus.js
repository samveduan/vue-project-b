//footerStatus.js

const state = {
  name: 'footerStatus'
};

const getters = {   //实时监听state值的变化(最新状态)

};

const mutations = {

};

const actions = {

};

export default {
  namespaced: true, //用于在全局引用此文里的方法时标识这一个的文件名
  state,
  getters,
  mutations,
  actions
}
